# shellbot

shellbot is a simple tool which sends Telegram message with a command execution status when it finishes. `TELEGRAM_BOT_TOKEN` and `TELEGRAM_CHAT_ID` environment variables must be set before the tool using.
